import { useState } from "react";
import { useNavigate } from "react-router-dom";

const DashboardScreen = () => {
    return(
        <div>
            <text>this is dashboard sceen</text>
        </div>
    );
};

export default DashboardScreen;